#include <stdio.h>
#include "main.h"

/**
 * add - adds two integers and returns the result.
 *@a: First int parameter
 *@b: Second int parameter
 *
 * Return: an integer
 */
int add(int a, int b)
{
return (a + b);
}
