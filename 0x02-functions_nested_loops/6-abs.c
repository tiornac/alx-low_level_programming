#include <stdio.h>
#include <ctype.h>
#include <stdlib.h>
#include <time.h>
#include "main.h"
/**
 * _abs - computes the absolute value of an integer.
 * @n: The number to check
 *
 * Return: an integer
 */
int _abs(int n)
{
int ch = n;
return (abs(ch));
}
