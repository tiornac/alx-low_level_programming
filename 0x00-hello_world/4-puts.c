#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char FirstPrint[] = "\"Programming is like building a multilingual puzzle";
puts(FirstPrint);
return (0);
}
